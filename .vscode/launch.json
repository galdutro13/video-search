{
    "configurations": [
        {
            "type": "node-terminal",
            "name": "Executar Script: dev",
            "request": "launch",
            "command": "npm run dev",
            "cwd": "${workspaceFolder}\\backend"
        }
    ]
}